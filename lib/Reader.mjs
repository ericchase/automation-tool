export class Reader {
  constructor() {}

  /**
   * Copies next `buffer.length` bytes from internal buffer into `buffer`,
   * and returns the number of bytes copied.
   * @this {Reader}
   * @param {Uint8Array} buffer
   * @return {number} bytes copied
   */
  read(buffer) {
    return 0;
  }
}
