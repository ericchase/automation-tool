import { ICommand } from './Doto/Command/Command';

// TODO:
function ProcessCommands(commands: ICommand[]) {}

// TODO:
function ParseCommandLine(commandLine: string) {
  const commands: ICommand[] = [];
  return commands;
}
