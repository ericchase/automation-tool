parser is actually a tokenizer, so change names again

change output of parser

parser should output list of commands

combine nonwhitespace characters into tokens
combine whitespace characters into tokens
